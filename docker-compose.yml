services:
    market-python-service:
        build: .
        container_name: market-python-service
        ports:
            - '8000:8000'
        env_file:
            - .env
        restart: unless-stopped
